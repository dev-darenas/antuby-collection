= render 'components/breadcrumb',
         links: [[invoices_sales_path, 'Facturas de Venta'],
          [edit_invoices_sale_path(@sale), @sale.code],
          ['Actividades']]
hr
.row
  .col-md-3
    .card
      .card-body
        p.card-text
          h3 = @sale.third_name
          br = @sale.third_email

          hr
          = render '/components/activities_links'
    hr
    .accordion id="userAccordion"
      .card
        .card-header id="headingUserInfo"
          h2.mb-0
            button.btn.btn-link data-toggle="collapse" data-target="#collapseInfo" aria-expanded="true" aria-controls="collapseInfo"
              | Información
        .collapse aria-labelledby="headingUserInfo" data-parent="#userAccordion" id="collapseInfo"
          .card-body
            br = @sale.third_cellphone
            br = @sale.third_phone
            br = @sale.third_address_description

      .card
        .card-header id="headingContacts"
          h2.mb-0
            button.btn.btn-link data-toggle="collapse" data-target="#collapseContacts" aria-expanded="true" aria-controls="collapseContacts"
              | Contactos
        .collapse aria-labelledby="headingContacts" data-parent="#userAccordion" id="collapseContacts"
          .card-body
            - @sale.third.contacts.each do |contact|
              = render contact

  .col-md-9
    ul.nav.nav-tabs
      li.nav-item
        = link_to 'Actividades', invoices_sale_activities_path(@sale), class: 'nav-link active'
      li.nav-item
        = link_to 'Llamadas', invoices_sale_activities_calls_path(@sale), class: 'nav-link'
      li.nav-item
        = link_to 'Email', invoices_sale_activities_emails_path(@sale), class: 'nav-link'
      li.nav-item
        = link_to 'Promesas de Pago', invoices_sale_activities_payment_promises_path(@sale), class: 'nav-link'
      li.nav-item
        = link_to 'Tareas', invoices_sale_activities_todos_path(@sale), class: 'nav-link'
    .tab-content
      - if @activities.exists?
        - @activities.each do |activity|
          = render activity
      - else
        .alert.alert-secondary role="alert"
          | No se han reportado actividades, presiona <a href="#" class="alert-link">aqui</a> para crear una.
